name: 5분마다 모니터링 실행
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: # 수동 실행 버튼
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: API 호출
        run: |
          curl -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" https://penta-works-web.vercel.app/api/monitor
